import 'package:flutter/material.dart';
import 'package:saveme/enterance/login/components/body.dart';

class LogInScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
